# Web server and CORS
Flask==2.2.5
flask-cors==3.0.10

# Image and array processing
numpy==1.24.2
opencv-python==4.8.0.76  # may require extra config on ARM (see note below)

# Plotting
matplotlib==3.7.1

# Raspberry Pi Camera support
picamera2 @ git+https://github.com/raspberrypi/picamera2.git

# Needed to avoid X server requirement in headless matplotlib
Pillow==9.5.0
